# Production Environment Configuration
# Instructions: Fill in the password and rename to .env

# Database Connection
DATABASE_URL=postgresql+asyncpg://dataset_curator:<PASSWORD>@datasetcurator-db.cedm8uygqls9.us-east-1.rds.amazonaws.com:5432/dataset_curator

# Security
# Generate a new key: openssl rand -hex 32
SECRET_KEY=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING

# LLM Configuration (From existing .env)
# OPENROUTER_API_KEY=...
# ... other config ...

# Storage
STORAGE_BACKEND=local
# Or S3 if configured
RAW_STORAGE_PATH=storage/raw
CURATED_STORAGE_PATH=storage/curated
